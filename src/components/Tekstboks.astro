---
const { text } = Astro.props;
---

<section>
  <p class="tekstboks">{text}</p>
</section>

<style>
  section {
    padding-block: var(--size-7);
    padding-inline: var(--size-5);
  }
  .tekstboks {
    font-size: var(--font-size-2);
    font-family: "futura-pt", sans-serif;
    font-weight: 400;
    font-style: normal;
  }
</style>
